part of '../utils.exports.dart';

extension ContextConversion on BuildContext {
  // void closeNotification() => InAppNotification.dismiss(context: this);

  // void startShowCase(GlobalKey key) =>
  //     ShowCaseWidget.of(this).startShowCase([key]);
  // void startShowCases(List<GlobalKey> keys) =>
  //     ShowCaseWidget.of(this).startShowCase(keys);
  // void dismissShowCase() => ShowCaseWidget.of(this).dismiss();
}
